version: 1
backend:
  prebuild:
    commands:
        - echo "Exporting environment variables"
        - "echo 'export AWS_ACCOUNT_ID=$AWS_ACCOUNT_ID' >> backend.env"
        - "echo 'export AWS_REGION=$AWS_REGION' >> backend.env"
        - "echo 'export DEVICE_TABLE_NAME=$DEVICE_TABLE_NAME' >> backend.env"
  phases:
    build:
      commands:
        - source backend.env
        - "echo 'AWS_ACCOUNT_ID after export: $AWS_ACCOUNT_ID'"
        - "echo 'AWS_REGION after export: $AWS_REGION'"
        - "echo 'DEVICE_TABLE_NAME after export: $DEVICE_TABLE_NAME'"
        # Install dependencies for backend
        - npm ci --cache .npm --prefer-offline
        # Install dependencies for each Lambda function
        - for dir in amplify/lambdas/*; do
            if [ -d "$dir" ]; then
              echo "Installing dependencies for $dir...";
              cd "$dir" && npm install && cd -;
            fi;
          done
        # Deploy the backend using ampx
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID --outputs-format dart
frontend:
  phases:
    preBuild:
      commands:
        # Install Flutter 3.24.5
        - wget https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.24.5-stable.tar.xz
        - tar xf flutter_linux_3.24.5-stable.tar.xz
        - export PATH="$PATH:`pwd`/flutter/bin"
        # Verify Flutter installation
        - flutter doctor
        # Clean and fetch dependencies
        - flutter clean
        - flutter pub get
    build:
      commands:
        - flutter build web
  artifacts:
    baseDirectory: build/web
    files:
      - '**/*'
  cache:
    paths:
      - flutter/.pub-cache
      - node_modules
